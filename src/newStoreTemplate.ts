import { StorefrontPages } from './enums/paths';

if (window.location.pathname === StorefrontPages.ADDTOCART) {
  alert('Add to Cart');
}

if (window.location.pathname === StorefrontPages.CART) {
  alert('Cart');
}

if (window.location.pathname === StorefrontPages.CATALOG) {
  alert('Catalog');
}

if (window.location.pathname === StorefrontPages.CHECKOUTADDRESS) {
  alert('Checkout Address');
}

if (window.location.pathname === StorefrontPages.CHECKOUTCONFIRMATION) {
  alert('Checkout Confirmation');
}

if (window.location.pathname === StorefrontPages.CHECKOUTPAYMENT) {
  alert('Checkout Payment');
}

if (window.location.pathname === StorefrontPages.CHECKOUTREVIEW) {
  alert('Checkout Review');
}

if (window.location.pathname === StorefrontPages.CHECKOUTSHIPPING) {
  alert('Checkout Shipping');
}

if (window.location.pathname === StorefrontPages.CREATEEDITACCOUNT) {
  alert('Create/Edit Account');
}

if (window.location.pathname === StorefrontPages.CUSTOMIZE) {
  alert('Customize Product');
}

if (window.location.pathname === StorefrontPages.MYACCOUNT) {
  alert('My Account');
}

if (window.location.pathname === StorefrontPages.VIEWORDERS) {
  alert('View Orders');
}
